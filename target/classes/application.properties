# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/eventsdb
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=rifqi080203

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
app.jwtSecret=MyVerySecureJwtSecretKeyThatIsAtLeast256BitsLongForHMACSecurityRequirements
app.jwtExpirationMs=86400000

# PostgreSQL Database Configuration (H2 Console removed)
